{{#if posts.length}}
<section>
  <h2>Recent Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postdata post}}
    {{/each}}

</section>
{{/if}}

<button type="submit" class="btn"><a class="link" href="/dashboard/new">Add Post</a></button>